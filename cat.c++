#include <iostream>
using namespace std;

int main() {
    float karet, sintetis;
    cin >> karet >> sintetis;

    float total = (karet * 22000) + (sintetis * 18000);

    cout << "Total biaya: " << total;
}